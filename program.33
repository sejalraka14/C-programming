#include<stdio.h>
#include<conio.h>
void main()
{
    int a[100], n, i, sum=0;
    clrscr();
    printf("Enter size of array: ");
    scanf("%d", &n);
    printf("Enter %d elements:\n", n);
    for(i=0; i<n; i++)
    {
        scanf("%d", &a[i]);
        sum += a[i];
    }
    printf("Sum of array elements = %d", sum);
    getch();
}
