#include<stdio.h>
#include<conio.h>
void main()
{
    int a, b, prod;
    clrscr();
    printf("Enter two integers: ");
    scanf("%d%d", &a, &b);
    prod = a * b;
    printf("Product = %d", prod);
    getch();
}
