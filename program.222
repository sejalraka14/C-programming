#include <stdio.h>
int main(){
    int a[5], i=0, sum=0;
    printf("Enter 5 readings: ");
    while(i<5){
        scanf("%d",&a[i]);
        sum+=a[i];
        i++;
    }
    printf("Average = %.2f", (float)sum/5);
    return 0;
}
