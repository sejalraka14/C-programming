#include <stdio.h>
float sum(int a[], int n){
    if(n == 0) return 0;
    return a[n-1] + sum(a, n-1);
}
int main(){
    int r[5];
    printf("Enter 5 readings: ");
    for(int i=0;i<5;i++) scanf("%d",&r[i]);
    printf("Average = %.2f", sum(r,5)/5);
    return 0;
}
