#include<stdio.h>
#include<conio.h>
void main()
{
    int level;
    clrscr();
    printf("Enter water tank level (%): ");
    scanf("%d",&level);
    if(level<20)
        printf("Pump ON");
    else if(level>=20 && level<=80)
        printf("Pump Standby");
    else
        printf("Tank Full - Pump OFF");
    getch();
}
