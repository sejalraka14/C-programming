#include<stdio.h>
#include<conio.h>
void main()
{
    int n, i;
    long prod = 1;
    clrscr();
    printf("Enter n: ");
    scanf("%d", &n);
    for(i=1; i<=n; i+=2)
        prod *= i;
    printf("Product of odd numbers = %ld", prod);
    getch();
}
