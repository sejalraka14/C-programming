#include<stdio.h>
#include<conio.h>
void main()
{
    float a,b;
    clrscr();
    printf("Enter two numbers: ");
    scanf("%f%f",&a,&b);
    if(b!=0)
        printf("Division = %.2f",a/b);
    else
        printf("Division by zero not allowed!");
    getch();
}
