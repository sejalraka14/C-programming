#include<stdio.h>
#include<conio.h>
void main()
{
    int n, prod=1, digit;
    clrscr();
    printf("Enter a number: ");
    scanf("%d", &n);
    while(n > 0)
    {
        digit = n % 10;
        prod *= digit;
        n = n / 10;
    }
    printf("Product of digits = %d", prod);
    getch();
}
