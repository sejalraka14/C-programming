#include<stdio.h>
#include<conio.h>
void main()
{
    int n, sum=0, i;
    clrscr();
    printf("Enter n: ");
    scanf("%d", &n);
    for(i=1; i<=n; i++)
        sum += i;
    printf("Sum = %d", sum);
    getch();
}
