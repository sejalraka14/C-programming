#include <stdio.h>
int main(){
    int x,sum=0,count=0;
    printf("Enter readings (-999 to stop): ");
    while(1){
        scanf("%d",&x);
        if(x==-999) break;
        sum+=x; count++;
    }
    printf("Average = %.2f", count? (float)sum/count : 0);
    return 0;
}
