#include<stdio.h>
#include<conio.h>
void main()
{
    int temp;
    clrscr();
    printf("Enter temperature (°C): ");
    scanf("%d", &temp);
    if(temp > 30)
        printf("Fan ON");
    else
        printf("Fan OFF");
    getch();
}
