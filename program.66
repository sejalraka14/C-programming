#include<stdio.h>
#include<conio.h>
void main()
{
    float a, b, avg;
    clrscr();
    printf("Enter two numbers: ");
    scanf("%f%f", &a, &b);
    avg = (a + b) / 2;
    printf("Average = %.2f", avg);
    getch();
}
