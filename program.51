#include<stdio.h>
#include<conio.h>
void main()
{
    int n, i;
    long fact = 1;
    clrscr();
    printf("Enter n: ");
    scanf("%d", &n);
    for(i=1; i<=n; i++)
        fact *= i;
    printf("Factorial = %ld", fact);
    getch();
}
