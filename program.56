#include<stdio.h>
#include<conio.h>
void main()
{
    int a, b;
    float div;
    clrscr();
    printf("Enter two integers: ");
    scanf("%d%d", &a, &b);
    if(b != 0)
    {
        div = (float)a / b;
        printf("Division = %.2f", div);
    }
    else
        printf("Division by zero not allowed!");
    getch();
}
