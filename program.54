#include<stdio.h>
#include<conio.h>
void main()
{
    int a[100], n, i;
    long prod = 1;
    clrscr();
    printf("Enter size of array: ");
    scanf("%d", &n);
    printf("Enter %d elements:\n", n);
    for(i=0; i<n; i++)
    {
        scanf("%d", &a[i]);
        prod *= a[i];
    }
    printf("Product of array elements = %ld", prod);
    getch();
}
