#include<stdio.h>
#include<conio.h>
void main()
{
    int a, b, q, r;
    clrscr();
    printf("Enter two integers: ");
    scanf("%d%d", &a, &b);
    if(b != 0)
    {
        q = a / b;
        r = a % b;
        printf("Quotient = %d, Remainder = %d", q, r);
    }
    else
        printf("Division by zero not allowed!");
    getch();
}
