#include <stdio.h>
#define AVG(sum,n) ((float)(sum)/(n))
int main(){
    int s[5], sum=0;
    printf("Enter 5 readings: ");
    for(int i=0;i<5;i++){ scanf("%d",&s[i]); sum+=s[i]; }
    printf("Average = %.2f", AVG(sum,5));
    return 0;
}
