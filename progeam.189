#include <stdio.h>
float avgPtr(int *p,int n){
    int sum=0;
    for(int i=0;i<n;i++)
        sum+=*(p+i);
    return (float)sum/n;
}
int main(){
    int r[5];
    printf("Enter 5 readings: ");
    for(int i=0;i<5;i++) scanf("%d",&r[i]);
    printf("Average = %.2f", avgPtr(r,5));
    return 0;
}
