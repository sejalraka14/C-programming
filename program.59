#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
void main()
{
    int a, b;
    float div;
    clrscr();
    printf("Enter two integers: ");
    scanf("%d%d", &a, &b);
    if(a != 0 && b != 0)
    {
        div = (float)abs(a) / abs(b);
        printf("Absolute division = %.2f", div);
    }
    else
        printf("Division by zero not allowed!");
    getch();
}
