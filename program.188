#include <stdio.h>
int main(){
    int val,count=0,sum=0;
    printf("Enter readings (0 to stop): ");
    while(1){
        scanf("%d",&val);
        if(val==0) break;
        sum+=val; count++;
    }
    printf("Average = %.2f", count? (float)sum/count : 0);
    return 0;
}
