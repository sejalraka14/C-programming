#include<stdio.h>
#include<conio.h>
void main()
{
    int a, b, diff;
    clrscr();
    printf("Enter two integers: ");
    scanf("%d%d", &a, &b);
    diff = a - b;
    printf("Difference = %d", diff);
    getch();
}
