#include<stdio.h>
#include<conio.h>
void main()
{
    float a, b, diff;
    clrscr();
    printf("Enter two floats: ");
    scanf("%f%f", &a, &b);
    diff = a - b;
    printf("Difference = %.2f", diff);
    getch();
}
