#include<stdio.h>
#include<conio.h>
void main()
{
    float a,b;
    clrscr();
    printf("Enter two numbers: ");
    scanf("%f%f",&a,&b);
    printf("Sum=%.2f, Difference=%.2f, Product=%.2f, ",a+b,a-b,a*b);
    if(b!=0)
        printf("Quotient=%.2f",a/b);
    else
        printf("Quotient=Division by zero!");
    getch();
}
